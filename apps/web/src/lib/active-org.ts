export const ACTIVE_ORG_COOKIE = "nex_active_org";

export type WorkspaceSummary = {
  organization_id: string;
  organization_name: string;
  role: string;
  member_id: string;
  joined_at: string;
};
