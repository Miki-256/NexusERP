/** Post-auth page route — handles GET and RSC POST navigations after sign-in. */
export const POST_AUTH_BOOTSTRAP_PATH = "/workspace-bootstrap";

/** Legacy API alias kept for old links; always 303 → page route. */
export const LEGACY_POST_AUTH_BOOTSTRAP_API = "/api/workspace/bootstrap";
